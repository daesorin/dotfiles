# 1. BLOCK EVERYTHING
/*

# 2. ALLOW THE CONFIG ITSELF
!.gitignore

# 3. THE GITHUB SEVEN (Your Whitelist)
!hypr/
!kitty/
!nvim/
!rofi/
!tmux/
!waybar/
!zsh/

# 4. SPECIAL EXCLUSIONS (Safety)
# Explicitly ignore history files inside allowed folders
zsh/.zhistory
tmux/plugins/
