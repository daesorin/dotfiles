#!/usr/bin/env bash

SESSION=$(secret-tool lookup service bitwarden 2>/dev/null)

if [[ -z "$SESSION" ]]; then
  SESSION=$(bw unlock --raw)
  secret-tool store --label="bw-session" service bitwarden <<< "$SESSION"
fi

exec /usr/bin/bw "$@" --session "$SESSION"
